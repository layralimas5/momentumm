# Aura — variáveis de ambiente (cliente). Copie para .env.local e preencha.
# NUNCA commitar .env.local. Apenas chaves públicas (anon) vão aqui — o
# service_role JAMAIS deve aparecer no front-end.

# Encontre em: Supabase > Project Settings > API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
